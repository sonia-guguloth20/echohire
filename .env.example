DATABASE_URL=postgresql+psycopg2://echohire:echohire_dev_pw@db:5432/echohire
SECRET_KEY=change_me_to_something_random
ENVIRONMENT=development

# AWS (fill in when you get to the deployment phase)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
S3_BUCKET_NAME=echohire-reports

# Min cohort size before aggregate bias stats are computed (protects re-identification)
MIN_COHORT_SIZE=5
